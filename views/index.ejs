<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calviora</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet"/>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex flex-col min-h-screen">

<!-- Header -->
<%- include('./partials/header') %>

<!-- Main Content -->
<main class="flex-grow flex mt-[4.5rem] items-center justify-center px-6 pb-6 sm:py-12">

    <div class="w-full max-w-4xl flex items-center justify-center">

        <!-- Message -->
        <% if (message.length > 0) { %>
            <span class="absolute top-14 left-1/2 transform -translate-x-1/2 bg-red-100 text-red-700 border border-red-400 rounded px-4 py-3 text-center z-10 shadow-lg block">
                <%= message %>
            </span>
        <% } %>

        <!-- Form Container: Register and Login Forms -->
        <div class="w-full flex flex-col md:flex-row gap-6">

            <!-- Left Column: Register Form -->
            <div class="w-full md:w-1/2 flex items-center justify-center">
                <div class="w-full px-6 md:px-10">
                    <h3 class="text-4xl mb-4">Welcome to <span class="text-blue-400 font-bold">Calviora</span></h3>
                    <h4 class="text-2xl mb-5 mt-2">Create Your Account</h4>
                    <form autocomplete="off" action="/users/register" method="post">
                        <div class="mb-4">
                            <input
                                    class="bg-zinc-100 block w-full px-4 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                                    type="text" placeholder="Full Name" name="fullName" aria-label="Full Name" required>
                        </div>
                        <div class="mb-4">
                            <input
                                    class="bg-zinc-100 block w-full px-4 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                                    type="email" placeholder="Email" name="email" aria-label="Email" required>
                        </div>
                        <div class="mb-6">
                            <input
                                    class="bg-zinc-100 block w-full px-4 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                                    type="password" placeholder="Password" name="password" aria-label="Password"
                                    minlength="6" required>
                        </div>
                        <button type="submit" class="bg-blue-500 block text-white mt-2 px-5 py-3 rounded-full w-full">
                            Create Account
                        </button>
                    </form>
                </div>
            </div>

            <!-- Right Column: Login Form -->
            <div class="w-full md:w-1/2 flex items-center justify-center">
                <div class="w-full px-6 md:px-10">
                    <h4 class="text-2xl mb-5 mt-2">Login to Your Account</h4>
                    <form autocomplete="off" action="/users/login" method="post">
                        <div class="mb-4">
                            <input
                                    class="bg-zinc-100 block w-full px-4 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                                    type="email" placeholder="Email" name="email" aria-label="Email" required>
                        </div>
                        <div class="mb-6">
                            <input
                                    class="bg-zinc-100 block w-full px-4 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                                    type="password" placeholder="Password" name="password" aria-label="Password"
                                    minlength="6" required>
                        </div>
                        <button type="submit" class="bg-blue-500 block text-white mt-2 px-5 py-3 rounded-full w-full">
                            Login
                        </button>
                    </form>
                </div>
            </div>

        </div>
    </div>
</main>

<!-- Footer -->
<%- include('./partials/footer') %>

</body>
</html>
